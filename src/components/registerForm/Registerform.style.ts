import { createStyles } from "@material-ui/styles";

const RegisterFormStyles = createStyles({
    root: {
      width: '100%',
      display: 'flex', 
      alignItems: 'center',
      justifyContent: 'center',
    },
    value: {
      width: '80%',
    },
})

export default RegisterFormStyles
